# 大模型选型实操指南

## —— 结合 GB/Z 201—2026《人工智能 大模型选型和应用指南》与自选关注点（数据收集、训练数据质量、回答限制、"荼毒"、公平、后门）

> 参考文件：根目录《国家标准人工智能-大模型选型和应用指南.pdf》（GB/Z 201—2026，2026-07-30 发布，全文已 OCR 至 `GBZ201-2026-全文OCR.txt`）。
> 该标准是**指导性技术文件**（GB/Z，非强制），给出选型全生命周期框架。它的视角偏"管理侧"（组织、流程、成本、运维），**没有直接回答你关心的对抗性问题**——模型是否被"荼毒"、后门、数据收集、训练数据挑过的、回答受限。这些需要你在标准框架的每个环节里主动加压，本文就是干这件事的。

---

## 一、先吃透标准的方法论（骨架）

标准第 5.2 条把选型分成 **5 个阶段**。你的关注点应嵌入每个阶段，而不是最后做一次"安检"：

| 阶段 | 标准要求（摘要） | 你要加的私货 |
|---|---|---|
| 1 选型准备（第6章） | 策略规划、组织/人员保障、现状评估、模型初选；先定场景再选型 | 写清数据分级：哪些数据绝不能出域 → 直接决定能否用云端 API |
| 2 数据准备（第7章） | 采集/存储/处理/数据集建设；去标识化（GB/T 37964）、个人信息保护（GB/T 35273） | 明确"我的数据喂给谁、是否参与对方训练"，写进合同 |
| 3 选型与优化（第8章） | 多维比测、提示词工程、RAG、微调（LoRA/QLoRA）、综合调优 | 用**自建评测集**比测，不用厂商给的榜单 |
| 4 部署应用（第9章） | MaaS / 私有化 / 智算中心三种模式；灰度发布、可信环境（SGX/TrustZone） | 数据敏感度 → 部署模式，一一对应 |
| 5 应用评估 + 风险管理（第10、11章） | 按 GB/T 45288 系列做模型/效益评估；应急预案、模型下线机制 | 把公平性、后门、内容限制的持续监测做成例行项 |

标准第 5.1 条的 6 项原则（合理性、适用性、适合性、可解释性、完善性、安全性）里，**可解释性**和**安全性**正是你关注点的抓手：可解释性帮助发现"偏差、歧视"（标准原文 5.1.4），安全性要求防数据泄露、算法被恶意利用（5.1.6）。

---

## 二、你的 6 个关注点 → 标准里的落点 + 自查动作

### 1. 国产大模型会收集数据吗？—— 数据出境/入域问题

**事实基础**：任何云端 API（不分国内外）都可能把你的请求用于改进服务，区别在于合同和开关。国内主流厂商（通义/阿里云、文心、混元、豆包、GLM、Kimi 等）均提供"关闭训练用途"的选项或企业版承诺，但**免费个人版默认多半是收集的**。国产模型反而有一优势：数据不出境，规避《数据出境安全评估办法》的问题。

**自查动作**：
- 读隐私政策里"改进模型/用于训练"条款，企业版拿书面承诺（不训练、不留存、可删除）
- 敏感数据走私有化部署或本地小模型，云 API 只放通用/脱敏流量（对应标准 9.1：数据安全要求高 → 私有化）
- 打日志审计：默认不把业务上下文写进第三方 SDK

### 2. 训练数据是精挑细选的 —— 分布偏差问题

"精挑细选"= 训练分布 ≠ 真实分布。模型会系统性偏向被放大的语料（如某类立场、某类文风、过于"干净正确"的表述）。

**自查动作**（对应标准 7.1 f：建立数据偏差检测机制——统计检验、公平性指标；7.3 数据清洗、质量评估）：
- 用你业务里的**真实长尾样本**（方言、错别字、行业黑话、极端案例）测模型，而不是标准问句
- 对比模型在"中位样本 vs 边缘样本"上的表现差距，差距大说明分布偏了
- 记住 RAG/微调（标准 8.3）是用你自己的数据重新"配平"的主要手段——这正是标准建议 RAG 的原因之一：回答垂直、封闭领域问题更可控

### 3. 回答是有限制的 —— 能力边界 vs 对齐策略

所有模型都有内容边界，这是产品决策不是缺陷。关键是**分清两类限制**：
- **无害的限制**：拒答涉暴、违法请求 → 影响不大
- **有害的能力阉割**：把某些正常领域（如医疗建议边界、时事、历史、竞品对比）也答得含糊或明显偏转 → 影响业务正确性

**自查动作**：
- 用 50~100 条业务里**敏感度居中**的问题（不是越界问题，是"接近边界"的正常问题）测各家，看谁答得既合规又不失真
- 测"拒答率虚高"：把无关问题改写后看是否误触发安全策略（对客服类业务尤其重要）
- 立场性测试：同一争议话题换 3 种问法，看答案是否稳定（漂移大 = 对齐层干预重，做客观分析类业务要小心）

### 4. 模型是否被"荼毒"（投毒/偏见注入）

分两层：
- **预训练投毒**：训练语料被塞入错误知识。表现：某类事实性错误高度一致、引用不存在的来源
- **微调/对齐阶段的立场注入**：模型在开放讨论中系统性偏向某种叙事

**自查动作**：
- 事实抽测：在**你领域内**出 50 道有标准答案的题（含数字、日期、法规条文），错误模式若集中在同一主题/同一方向 → 警惕
- 交叉验证：同一问题问 2~3 家模型 + 传统搜索引擎，系统性分歧处就是"荼毒"高发区
- 标准第 8.2 g 条"道德风险管控：设不少于 3 人的复核小组"——把"识别模型系统性偏向"写进复核小组职责

### 5. 公平性

标准 5.1.4 明确提到要用可解释技术发现"偏差、歧视"，7.1 f 提到"公平性指标"。

**自查动作**：
- **反事实测试**：只换人群标签（性别/地域/年龄/方言），其他全同，看输出差异（对招聘、风控、客服话术类业务必做）
- 统计公平性指标：各群体上的通过率/拒绝率/得分差（如 demographic parity 差距 < 5% 起步）
- 训练数据侧（若自训）：按群体统计样本量与标注质量（标准 7.3：样本不均衡问题）

### 6. 后门（隐藏触发行为）

现实风险评估：**API 闭源模型**你无法验证，只能靠行为抽测 + 供应商信誉 + 合同约束；**开源/自部署模型**可做更实质的检查。

**自查动作**：
- 触发词扫描：构造含可疑 token（特定人名、编号、URL、乱码）的输入，观察行为突变
- API 模型：定期跑固定回归集（同一批题每月跑一次），答案分布突变 = 模型被悄悄换过/更新过（这也是"模型下线或更替机制"（标准 11.4 c）的监测基础）
- 供应链：只从官方渠道拿权重，校验哈希；推理框架用主流版本并关注 CVE
- 合同里写明：版本变更提前通知（对应闭源 API 模型"悄悄升级"问题）

---

## 三、部署模式 × 数据敏感度对照（标准 9.1 具体化）

| 数据敏感度 | 推荐部署 | 可选模型形态 | 你关注点的解法 |
|---|---|---|---|
| 公开/通用流量 | MaaS API | 任意国产旗舰 API | 隐私政策审查 + 关训练开关即可 |
| 内部业务数据 | 私有 VPC 部署或专有云 | 厂商企业版（签 DPA） | 合同写死：不训练、不留存、可审计 |
| 核心/涉密 | 完全离线私有化 | 开源权重（Qwen、DeepSeek、GLM 系）自部署 | 自己控制训练/微调链路，"荼毒"风险只剩权重本身，可离线评测 |
| 端侧/边缘 | 量化小模型（7B/14B，量化后 8~10GB 显存） | 开源 + GPTQ/AWQ 量化 | 数据永不外传，公平性靠自己的评测集持续测 |

标准附录 A.5 给的量化参考（表 A.3 原文）：8-bit 量化显存压缩率 50%（性能损失 < 2%），4-bit GPTQ 压缩率 75%（损失 < 5%，适用边缘设备），ZeRO Offload 压缩率 60%~80%（延迟增加 20%，可低成本单卡训练 70B）。

> 2026-09-09 据 PDF 原文（p19 表 A.3）更正：此前把 4-bit GPTQ 误记为「省 60~80%」，60~80% 实为 ZeRO Offload 的数值；4-bit GPTQ 为 75%。

---

## 四、可打分的选型评测表（把标准 6.5 / 8.3 的"多轮降配验证 + 比测"落地）

对每个候选模型按 1~5 打分，加权求和：

| 维度 | 权重建议 | 测什么 |
|---|---|---|
| 业务任务准确率 | 25% | 自建 200+ 条业务评测集（含长尾样本），不用公开榜单 |
| 公平性 | 15% | 反事实测试差异、群体指标差 |
| 抗"荼毒"/事实一致性 | 15% | 领域事实抽测错误率、错误是否系统性集中 |
| 回答可用性（对齐损耗） | 10% | 敏感度居中问题的拒答虚高率、立场漂移 |
| 数据合规 | 15% | 隐私政策、训练用途开关、合同承诺、部署选项 |
| 成本（标准 6.5 c） | 10% | 每千次调用成本 or 私有化摊销；训练/推理/运维（A.1 显存公式：`M_VM = (P × q / 8) × 1.2`，P 为参数规模、q 为量化位数，单位 GB） |
| 生态与续命能力（标准 5.1.5 完善性） | 10% | 版本迭代节奏、API 稳定性、是否提供私有化路径、社区 |

> 2026-09-09 据 PDF 原文（p17 公式 A.1）更正：显存公式为 `M_VM = (P × q / 8) × 1.2`（是除以 8，不是乘 8）。按旧写法 7B/16bit 会算出 1075 GB，实际约 16.8 GB。
>
> 权重按你的场景调：涉密场景把"数据合规"提到 30%+；面向公众的业务把"公平性"提到 20%+。

---

## 五、给供应商的问题清单（招标/POC 时直接甩出去）

1. 用户输入是否默认用于训练？企业版能否书面承诺不训练、不留存、可删除？
2. 模型上次更新时间？版本变更是否提前 N 天通知？旧版本保留多久？
3. 是否提供私有化部署？最低硬件配置？许可条款（商用限制）？
4. 训练数据的来源构成和清洗流程能否披露（哪怕是概要）？
5. 有无第三方安全评估（对齐/越狱/后门检测）报告？
6. 内容边界的具体清单能否提供？误拒答率有无内部统计？
7. SLA 与数据隔离方案（多租户怎么隔离）？
8. 出问题时（模型给出有害/错误输出）的责任划分和应急配合流程（对应标准 11.4）？

---

## 六、持续监测（选型不是一次性决定）

标准第 10~11 章要求定期评估 + 应急预案，具体化：

- **月度**：固定回归评测集跑一遍，跟踪准确率/拒答率/公平指标漂移（也是闭源 API 换版本的探测手段）
- **季度**：抽新一轮真实业务样本做人工盲评；复核小组（≥3 人，标准 8.2 g）审一次系统性偏向
- **年度**：重跑完整选型评测表——大模型迭代快，去年的赢家今年可能被反超；同时演练一次"模型下线/更换"流程（标准 11.4 c）
- **事件驱动**：答案分布突变、拒答率跳变、新群体性错误 → 触发应急预案（下线/隔离/回退版本）

---

## 七、一页总结

1. **先定数据敏感度 → 部署模式**，这一步直接消解"数据收集"焦虑的大半
2. **用自建评测集比测**，公开榜单只做初筛（标准 6.5：基准测试 + 多轮降配验证）
3. **公平/荼毒/后门三件事 = 反事实测试 + 事实抽测 + 触发词扫描 + 回归监测**，全是可执行动作
4. **回答限制不是扣分项，是匹配项**：测它的边界是否与你的业务冲突
5. 合同与日志是你对闭源模型唯一的硬约束；开源权重才给你彻底的控制权
6. 标准的价值在**流程骨架**（准备→数据→选型→部署→评估→风控），你的关注点作为"加压项"嵌进每个环节
